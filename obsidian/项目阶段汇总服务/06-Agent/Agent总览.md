# Agent 总览

## 当前定位

Agent 是稳定执行器，不负责长期承载业务规则。

## 当前职责

- 解密主服务请求
- 本地连接 SQL Server / MySQL
- 按主服务下发的 `QueryDefinition` 执行查询
- 返回标准化记录

## 当前不负责

- 页面展示
- 用户权限
- 负责人分配
- 应用服务器分配
- 中心表查询

## 当前设计目的

后期如果只增加阶段、字段、统计口径，尽量只更新主服务，不更新 Agent。
